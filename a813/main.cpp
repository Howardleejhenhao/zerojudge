#include <bits/stdc++.h>
using namespace std;
#define ll long long
vector<int>n;
int main()
{

    int num;

    cin >> num;
    int in;
    for(int i = 0; i < num; i++)
    {
        cin >> in;
        n.push_back(in);
    }
    int total = 0;

    for(int i = 0; i < n; i++)
    {

    }
    cout << "Hello world!" << endl;
    return 0;
}
