#include <bits/stdc++.h>
#define ll long long
#define ss stringstream
#define fastio ios::sync_with_stdio(0), cin.tie(0)

using namespace std;

int main()
{
    fastio;

    int t;
    cin >> t;
    while(t--)
    {
        int n, m;
        cin >> n >> m;
        int a[n][n];
        int ans = n;
        memset(a, 0, sizeof(a));
        int i = 1;
        int x = 0, y = 0, where = 1;//¥k¤U¥ª¤W
        if(m)
        {

        }

    }
    return 0;
}
