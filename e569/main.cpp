#include <bits/stdc++.h>
#define ll long long
#define fastio ios::sync_with_stdio(0), cin.tie(0)

#define f first
#define s second
#define pb push_back;

using namespace std;

int main()
{
    fastio;
    // freopen("input.txt", "r", stdin);
    // freopen("output.txt", "r", stdout);
    list<string> ls;
    string s;
    stringstream ss;
    while(getline(cin, s))
    {
        if(s[0] == '0') break;
        ss << s;
        string input;
        while(ss >> input)
        {
            for(int i = 0; )
        }


        ss.clear();
    }
    return 0;
}
