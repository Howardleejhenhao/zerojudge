#include <iostream>

using namespace std;

int main()
{
    int a, b, c;
    cin >> a >> b >> c;
    int mx = max(a, b);
    mx = max(mx, c);
    cout << mx << endl;
    return 0;
    int a = 0, b = 0, c = 0, d = 0;
}
