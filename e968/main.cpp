#include <bits/stdc++.h>

using namespace std;

int main()
{
    int num;
    int num_list[num + 1];
    int in;
    cin >> num;
    int a, b, c;
    cin >> a >> b >> c;
    for(int i = num; i > 0; i--)
    {
        if(i == a || i == b || i == c);
        else
        {
            cout << "No. " << i << endl;
        }
    }
    return 0;
}
