#include <bits/stdc++.h>
#define ll long long
#define ss stringstream
#define fastio ios::sync_with_stdio(0), cin.tie(0)

using namespace std;

int main()
{
    fastio;
    int n, m;
    cin >> n >> m;

    string s;
    getline(cin, s);
    string str[n];
    for(int i = 0; i < n; i++)
    {
        getline(cin, str[i]);
    }
    for(int i = 0; i < n - 2; n++)
    {
        for(int j = 1; j < m - 1; j++)
        {
            if(str[])
        }
    }

    return 0;
}
